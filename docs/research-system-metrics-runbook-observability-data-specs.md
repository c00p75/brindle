{"prompt":"Executive summary (primary-source citations)\n\nThis research-system doc makes strategy development enforceable in a Cursor/LLM workflow by binding rules to concrete mechanisms in the chosen stack: FastAPI’s official testing guidance encourages pytest + in-process `TestClient` for reliable API tests, and FastAPI’s `app.dependency_overrides` (a plain dict) provides a mechanical way to swap real dependencies for fakes during tests—critical for preventing real exchange/network I/O in research runs. citeturn0search0turn0search1 Next.js’ environment-variable rules (including the `NEXT_PUBLIC_` convention for values bundled into the browser) support an enforceable “no secrets in client-side config” policy. citeturn0search2 CCXT’s async examples (`ccxt.async_support`, `fetch_ticker`, and `exchange.close()`) support a clean runtime-only market-data adapter boundary, keeping strategy tests deterministic and offline. citeturn0search3 Pytest’s `monkeypatch.setenv`/`monkeypatch.delenv` is the canonical mechanism for testing safety gates (paper-only flags) without leaking state across tests. citeturn0search4 Vitest’s official mocking tools (`vi.fn`, `vi.mock`, `vi.spyOn`) enable deterministic frontend research dashboards (e.g., metrics pages) without real network dependencies. citeturn0search5\n\n```docs/RESEARCH_SYSTEM.md\n# RESEARCH SYSTEM — Strategy Development Pipeline (Paper-First, Test-First)\n\nDate: 2026-04-04 (Africa/Lusaka)  \nStatus: Active  \nCI/CD provider: UNSPECIFIED  \nBacktest/replay engine: UNSPECIFIED  \nStrategy scheduler/runner: UNSPECIFIED\n\n## Purpose\nCreate a repeatable, auditable process to:\n- form strategy hypotheses,\n- implement strategies safely (paper-only),\n- evaluate results with pre-defined criteria,\n- and prevent common research errors (lookahead, leakage, overfitting).\n\n## Scope\nIn scope:\n- Paper-first strategy research workflow (spec → tests → implementation → experiment → evaluation).\n- Required artefacts per strategy and per run.\n- Bias-control checklist.\n\nOut of scope:\n- Live trading (forbidden).\n- Exchange-specific private endpoints (forbidden).\n- Full backtesting infra/tool choice (UNSPECIFIED).\n\n## Non-negotiable rules\n- PAPER TRADING ONLY: strategies must place orders via the paper execution boundary only.\n- SINGLE EXECUTION BOUNDARY: strategies must call `ExecutionService.place_market_order(...)` (in-process) OR POST `/paper/orders/market` (HTTP). Never call CCXT directly.\n- NO NETWORK IN TESTS: strategy unit/integration tests must not touch the network. Use fakes/mocks.\n- TDD: failing tests first (Red → Green → Refactor). No strategy logic without a failing test.\n- DETERMINISTIC: given identical inputs (prices, timestamps, params), outputs must be identical.\n- SAFE FLAGS REQUIRED: `PAPER_TRADING_ONLY=true` and `LIVE_TRADING_ENABLED=false` for all research runs.\n- NO SECRETS: no API keys or secrets in code, logs, or committed files.\n\n## Required artefacts per strategy (Definition of Done)\nFor every strategy `strategy_id` and `version`:\n\n### Strategy spec (required)\nA markdown document containing:\n- hypothesis (“why should this work?”)\n- instrument(s)/symbol(s)\n- entry/exit logic (high-level)\n- regime assumptions (when it should/shouldn’t work)\n- failure modes + explicit invalidation criteria\n- required risk constraints\n\n### Parameter schema (required)\nA machine-readable schema describing parameters:\n- name, type, unit\n- bounds\n- default (may be UNSPECIFIED until chosen)\n- description\n\nRecommended format: JSON schema or Pydantic model (choice UNSPECIFIED; must be consistent).\n\n### Tests (required)\nMinimum required tests:\n- Unit tests: strategy decision logic is pure and calls ONLY the execution boundary.\n- Integration tests: strategy + paper simulator + ledger (no network).\n\n### Run manifest (required)\nA single file per experiment run describing:\n- run_id\n- strategy_id + version\n- parameters\n- time window / steps (if replay)\n- data source identifiers (UNSPECIFIED)\n- code version identifier (commit hash: UNSPECIFIED)\n- safety flags (paper-only)\n\n### Results bundle (required)\nA directory output per run containing:\n- `metrics.json` (required schema: see METRICS.md)\n- `events.jsonl` or log summary (format UNSPECIFIED)\n- optional charts (UNSPECIFIED)\n\n## Step-by-step research lifecycle (required)\n1) Write strategy spec (hypothesis + failure modes + risk constraints).\n2) Define acceptance criteria BEFORE running experiments (thresholds may be UNSPECIFIED but must be written).\n3) Define parameter schema (bounds + defaults).\n4) Write failing unit tests for:\n   - trigger conditions\n   - execution boundary calls\n   - state progression (timers/counters)\n   - risk blocks (no order when violated)\n5) Implement minimum strategy code to pass tests.\n6) Add integration tests against paper simulator + ledger.\n7) Run experiments (paper run or replay) using a run manifest.\n8) Produce results bundle (`metrics.json` + log summary).\n9) Evaluate vs acceptance criteria and record conclusion:\n   - ACCEPT (paper unattended)\n   - ITERATE (change hypothesis/params)\n   - REJECT (document why)\n10) Post-mortem: add at least one new test that would have caught the biggest failure.\n\n## Bias-control checklist (must be completed per run)\nLookahead & leakage\n- [ ] Strategy step only reads data available at that timestamp (no future candles/ticks).\n- [ ] No “close” price is used before the candle closes (if using candles).\n- [ ] No parameter tuning on the same window used for final performance evaluation.\n\nSelection & survivorship bias\n- [ ] Symbol/universe selection is stated (UNSPECIFIED allowed) and consistent.\n- [ ] If symbols change over time, the rule is documented (UNSPECIFIED).\n\nExecution realism\n- [ ] Fees are included.\n- [ ] Slippage model: UNSPECIFIED (must be stated; if absent, results are labelled “fee-only”).\n- [ ] Stale-data handling is enforced (block orders when stale).\n\nRobustness\n- [ ] Test on at least 2 regimes (definition UNSPECIFIED but must be written).\n- [ ] Sensitivity check: small parameter changes do not collapse results.\n\n## Acceptance criteria template (thresholds are UNSPECIFIED)\nFill this BEFORE experiments:\n- Minimum run length / sample size: UNSPECIFIED\n- Max drawdown limit (%): UNSPECIFIED\n- Net expectancy after fees: > 0\n- Return (%): UNSPECIFIED\n- Operational error rate (rejects/errors): < UNSPECIFIED\n- Regime robustness requirement: UNSPECIFIED\n- Reason to stop strategy early: UNSPECIFIED\n\n## Required templates/snippets\n\n### Strategy research ticket (copy/paste)\n- Strategy name / id:\n- Hypothesis:\n- Symbols/instruments:\n- Entry signals:\n- Exit signals:\n- Required risk constraints:\n- Failure modes:\n- What would disprove it?:\n- Parameter schema summary:\n- Data requirements:\n- Acceptance criteria (pre-filled):\n\n### Run manifest template (YAML example; format may be changed but must be consistent)\n```yaml\nrun_id: run_YYYYMMDD_001\nstrategy_id: dca_btc\nstrategy_version: v0\npaper_trading_only: true\nlive_trading_enabled: false\nsymbols:\n  - BTC/USDT\nparams:\n  interval_seconds: 60\n  quote_amount_per_step: 50.0\n  max_total_quote_spend: 200.0\nwindow:\n  start_ms: UNSPECIFIED\n  end_ms: UNSPECIFIED\ncode_version: UNSPECIFIED\ndata_source: UNSPECIFIED\n```\n\n### Pytest: enforce execution boundary usage + env safety flags (runnable pattern)\n```python\nimport pytest\n\ndef guard_paper_only(*, paper_trading_only: bool, live_trading_enabled: bool) -> None:\n    if not paper_trading_only or live_trading_enabled:\n        raise RuntimeError(\"Unsafe mode: paper trading only is required\")\n\nclass FakeExecution:\n    def __init__(self):\n        self.calls = []\n\n    def place_market_order(self, side: str, symbol: str, amount: float, price: float):\n        self.calls.append((side, symbol, amount, price))\n        return {\"status\": \"filled\"}\n\nclass ExampleStrategy:\n    # Minimal example: real strategies must accept dependencies via parameters (DI)\n    def __init__(self, *, symbol: str):\n        self.symbol = symbol\n\n    def step(self, *, now_ms: int, price_last: float, execution: FakeExecution):\n        # Example trigger: always buy 1 unit of quote amount\n        execution.place_market_order(\"buy\", self.symbol, 1.0, price_last)\n\ndef test_strategy_calls_execution_boundary_only():\n    exe = FakeExecution()\n    strat = ExampleStrategy(symbol=\"BTC/USDT\")\n    strat.step(now_ms=0, price_last=50_000.0, execution=exe)\n    assert exe.calls == [(\"buy\", \"BTC/USDT\", 1.0, 50_000.0)]\n\ndef test_env_flags_guard(monkeypatch):\n    # Use monkeypatch to simulate env config safely (does not leak between tests)\n    monkeypatch.setenv(\"PAPER_TRADING_ONLY\", \"true\")\n    monkeypatch.setenv(\"LIVE_TRADING_ENABLED\", \"false\")\n\n    paper = (monkeypatch.getenv(\"PAPER_TRADING_ONLY\") or \"\").lower() == \"true\"\n    live = (monkeypatch.getenv(\"LIVE_TRADING_ENABLED\") or \"\").lower() == \"true\"\n\n    guard_paper_only(paper_trading_only=paper, live_trading_enabled=live)\n\n    # Now flip to unsafe and assert guard triggers\n    monkeypatch.setenv(\"PAPER_TRADING_ONLY\", \"false\")\n    paper2 = (monkeypatch.getenv(\"PAPER_TRADING_ONLY\") or \"\").lower() == \"true\"\n    with pytest.raises(RuntimeError):\n        guard_paper_only(paper_trading_only=paper2, live_trading_enabled=False)\n```\n\n## Example run commands\nLocal (backend tests):\n- cd backend\n- pytest\n\nExperiment runner (tooling UNSPECIFIED; must accept a run manifest):\n- cd backend\n- python -m app.tools.run_strategy --manifest manifests/run_YYYYMMDD_001.yaml  # UNSPECIFIED module\n\n## Reviewer checks (must pass)\n- [ ] Strategy spec exists and states hypothesis + failure modes.\n- [ ] Acceptance criteria were written BEFORE running experiments.\n- [ ] Parameter schema exists with bounds; defaults may be UNSPECIFIED but must be explicit.\n- [ ] Unit tests prove strategy calls ONLY the execution boundary (no CCXT usage).\n- [ ] Integration tests run offline and use paper simulator + ledger.\n- [ ] Results bundle contains metrics.json and references the run manifest.\n- [ ] Any UNSPECIFIED policy choices are explicitly documented (not silently assumed).\n\n## Next steps\n- Define UNSPECIFIED thresholds for acceptance (drawdown, run length, error rate) and commit them.\n- Define the run manifest schema formally (JSON Schema or Pydantic; choose one).\n- Implement (or stub) the experiment runner entry point that consumes a manifest and produces `metrics.json`.\n```\n"}