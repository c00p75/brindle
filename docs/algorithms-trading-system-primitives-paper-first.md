{"prompt":"```docs/STRATEGY_GRAVEYARD.md\n# STRATEGY GRAVEYARD (append-only)\n\nDate: 2026-04-04 (Africa/Lusaka)  \nStatus: Active  \nRun artefact location: `experiments/<run_id>/` (path may be UNSPECIFIED).\n\n## Purpose\nRecord rejected/paused strategies and the evidence for rejection, to prevent repeating the same mistakes and to enforce disciplined promotion decisions.\n\n## Scope\nIn:\n- Rejection/pausing decisions with evidence.\n- “What would make us revisit this?” criteria.\nOut:\n- Live trading decisions (forbidden).\n- Re-arguing without new evidence.\n\n## Non-negotiable rules\n- Append-only: do not delete or rewrite history; add a new entry if something changes.\n- Evidence required: every entry references run_id(s) + `metrics.json`.\n- No resurrection without a fix + new tests addressing the original failure.\n- No secrets: never paste keys, tokens, or private URLs.\n\n## PR description format (required when adding an entry)\nTask:\nAssumptions (mark UNSPECIFIED if needed):\nPlan:\nFiles to create/change (exact paths):\nTests first (if disabling strategy):\nImplementation:\nRun commands:\nNext step:\n\n## Entry template (copy/paste; append under “Entries”)\n```text\nStrategy id:\nVersion:\nStatus: Rejected | Paused\nDate:\nWhy it failed (1–3 bullets):\nEvidence:\n- run_id:\n- metrics.json:\nBias checks completed: Yes/No\nWhat would change our mind:\nTests added (paths):\nReviewer:\n```\n\n## Reviewer checks (must pass)\n- [ ] Entry appended for every rejected/paused strategy (same PR).\n- [ ] Evidence includes at least one run_id and its `metrics.json`.\n- [ ] Failure labelled: bug vs no-edge vs regime mismatch vs data issue.\n- [ ] “What would change our mind” is actionable (not vague).\n- [ ] If disabled in code, tests ensure it is not enabled by default.\n\n## Example run commands\n- `ls experiments/<run_id>` (path UNSPECIFIED)\n- `cat experiments/<run_id>/metrics.json`\n\n## Next steps\n- Decide the canonical experiments folder layout (currently UNSPECIFIED).\n- Add an automated check to block promotion if an open graveyard entry exists.\n```\n```docs/EXPERIMENT_LOG.md\n# EXPERIMENT LOG (append-only)\n\nDate: 2026-04-04 (Africa/Lusaka)  \nStatus: Active  \nRunner: UNSPECIFIED (must consume a manifest and emit metrics.json)\n\n## Purpose\nMaintain a reproducibility index of every experiment run so results are traceable to parameters, code, data, and safety mode.\n\n## Scope\nIn:\n- One log entry per run_id (paper run or replay/backtest).\n- Links to manifests and results bundles.\nOut:\n- Ad-hoc “trust me” results without artefacts.\n\n## Non-negotiable rules\n- Append-only: never edit past entries; add a correction entry if needed.\n- A run is invalid unless it has: manifest + metrics.json + decision.\n- Safety flags must be recorded (paper-only must be true).\n- No secrets in the log.\n\n## Required entry template (append under “Entries”)\n```text\nrun_id:\ntimestamp_started_ms:\ntimestamp_ended_ms:\nstrategy_id:\nstrategy_version:\nmanifest_path:\nresults_path:\ncode_version: UNSPECIFIED\ndata_source: UNSPECIFIED\nsafety:\n  paper_trading_only: true\n  live_trading_enabled: false\ndecision: Accept | Iterate | Reject\nnotes (≤5 lines):\nreviewer:\n```\n\n## Example “change” output format (for PR descriptions)\nTask:\nAssumptions (mark UNSPECIFIED if needed):\nPlan:\nFiles to create/change (exact paths):\nTests first (list + key assertions):\nImplementation (each file as a path-labelled fenced code block):\nRun commands:\nNext step (1–3 items):\n\n## Snippet (pytest): validate metrics.json exists for a run folder\n```python\nfrom pathlib import Path\n\ndef test_run_has_metrics_json():\n    run_id = \"UNSPECIFIED\"\n    p = Path(\"experiments\") / run_id / \"metrics.json\"\n    assert p.exists(), f\"missing {p}\"\n```\n\n## Example run commands\n- Run (runner module UNSPECIFIED):\n  - `python -m app.tools.run_strategy --manifest manifests/<run_id>.yaml`\n- Record entry:\n  - append to `docs/EXPERIMENT_LOG.md`\n- Verify artefacts:\n  - `test -f experiments/<run_id>/metrics.json`\n\n## Reviewer checks (must pass)\n- [ ] New run adds one entry with run_id, manifest_path, results_path, decision.\n- [ ] metrics.json exists and follows METRICS.md exact-key rules.\n- [ ] Acceptance criteria were precommitted (link/reference; location UNSPECIFIED).\n- [ ] No secrets included.\n\n## Next steps\n- Choose a standard `run_id` format (e.g., run_YYYYMMDD_NNN) — UNSPECIFIED.\n- Implement a small CLI to append entries automatically (UNSPECIFIED).\n```\n```docs/TIME_HORIZONS.md\n# TIME HORIZONS — Evaluation Windows, Cadence, and Promotion Gates\n\nDate: 2026-04-04 (Africa/Lusaka)  \nTimestamps: UTC epoch ms  \nLive trading: forbidden until safety gates are met.\n\n## Purpose\nDefine time-based discipline so strategies are not promoted or killed based on short-term noise, and reviews happen on a fixed cadence.\n\n## Scope\nIn:\n- Minimum evaluation windows, sample sizes, and review cadence for paper runs.\n- Time-based kill conditions and cool-downs.\nOut:\n- Live trading timelines (forbidden).\n- Exact numeric thresholds (UNSPECIFIED; must be set per strategy/risk policy).\n\n## Non-negotiable rules\n- No promotion/rejection decision without BOTH minimum window AND minimum sample size (values may be UNSPECIFIED but must be stated).\n- Every decision references run_id + metrics.json.\n- Cool-down required after a halt/incident (duration UNSPECIFIED).\n- If code enforces horizons, it must be unit-tested.\n\n## Recommended horizons (fill values; currently UNSPECIFIED)\n- Warm-up/burn-in window: UNSPECIFIED\n- Minimum paper evaluation duration: UNSPECIFIED\n- Minimum trades count: UNSPECIFIED\n- Review cadence: UNSPECIFIED\n- Max time in “Iterate” without improvement: UNSPECIFIED\n- Cool-down after safety halt: UNSPECIFIED\n\n## Master-format decision note (paste into PR/issue)\nTask:\nAssumptions (mark UNSPECIFIED if needed):\nPlan:\nFiles to create/change (exact paths):\nTests first (if enforcing in code):\nImplementation:\n```text\nDecision type: Promote | Keep paper | Iterate | Reject\nDate:\nStrategy id + version:\nEvidence:\n- run_id:\n- metrics.json:\nAcceptance criteria location: UNSPECIFIED\nNotes (≤5 lines):\nReviewer:\n```\nRun commands:\nNext step:\n\n## Snippet (pytest): minimum elapsed time gate (pattern)\n```python\ndef can_promote(*, elapsed_ms: int, min_elapsed_ms: int) -> bool:\n    return elapsed_ms >= min_elapsed_ms\n\ndef test_can_promote_gate():\n    assert can_promote(elapsed_ms=1000, min_elapsed_ms=1000) is True\n    assert can_promote(elapsed_ms=999, min_elapsed_ms=1000) is False\n```\n\n## Example run commands\n- `cat experiments/<run_id>/metrics.json` (path UNSPECIFIED)\n- Weekly summary tool: UNSPECIFIED\n\n## Reviewer checks (must pass)\n- [ ] Decision references run_id and metrics.json.\n- [ ] Minimum window + sample size are stated (even if UNSPECIFIED values).\n- [ ] Any horizon-enforcement code includes unit tests.\n- [ ] Cool-down behaviour is documented in RUNBOOK.md.\n\n## Next steps\n- Set concrete defaults for UNSPECIFIED horizons (per risk policy).\n- Add automation to block promotion if horizons are not met.\n```"}