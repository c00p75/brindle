{"prompt":"# DISCIPLINE_AND_GOVERNANCE.md — Discipline, Governance, and Admin Configuration Controls\n\nDate: 2026-04-09 (Africa/Lusaka)\n\n## Purpose\nThis document defines the minimum discipline and governance required to operate a paper-trading-first trading-bot platform safely. It constrains who can change what, how changes are proposed/validated/applied, and what the UI must enforce so that “manual configuration whenever” does not become “unsafe configuration by accident”.\n\n## Scope\n\nIn:\n- Governance roles (Admin, Reviewer, Operator, Auditor) and responsibilities.\n- Configuration change workflow: draft → validate → apply → audit.\n- UI controls for safe admin configuration (draft vs applied, validation, rollback, audit trail).\n- On-call expectations and incident process (targets UNSPECIFIED).\n\nOut:\n- Live trading enablement (forbidden).\n- Organisational HR policies (UNSPECIFIED).\n- Specific CI/CD vendor/infra tooling (UNSPECIFIED).\n\n## Non-negotiable rules\n- Paper trading only: no live order placement, no withdrawal/transfer features.\n- No ad-hoc changes: all config changes go through the draft→validate→apply flow.\n- No bypassing safety gates: risk limits and paper-only locks cannot be overridden from UI.\n- TDD always: any behavioural change must include failing tests first.\n- No secrets in UI: secrets must never be displayed, exported, or logged.\n- Small diffs: change the minimum necessary; no unrelated refactors.\n\n## Governance roles & responsibilities\n\nAdmin\n- Creates and edits bot configuration drafts.\n- Initiates validation and proposes apply.\n- Cannot bypass paper-only mode or safety gates.\n\nReviewer\n- Reviews and approves configuration changes that affect strategy behaviour or risk.\n- Reviews code changes (PRs) and ensures tests/gates pass.\n\nOperator\n- Runs daily operations: monitoring, incident response, runbook execution.\n- Can pause/stop strategies and trigger kill switch (once implemented).\n\nAuditor\n- Reviews audit trails for configuration changes and production incidents.\n- Ensures decisions reference run artefacts (manifest/metrics/events).\n\nRole mapping to real people: **UNSPECIFIED**.\n\n## Configuration change workflow\n\nAuthoritative workflow (must be enforced by backend + UI):\n1) Draft\n- Admin edits a config draft with a new version id.\n- Draft is not active.\n\n2) Validate\n- Backend validates schema + constraints (types, bounds, invariants).\n- Validation is deterministic and produces a report.\n\n3) Apply\n- Applying creates a new “active configuration version” and freezes it.\n- Apply is atomic: either fully applied, or not applied.\n\n4) Audit\n- Every step writes an immutable audit record: who, what, when, diff, outcome.\n\n### Change request template (master prompt output format)\nUse this structure in PR descriptions and internal tickets:\n\nTask:\nAssumptions (mark UNSPECIFIED if needed):\nPlan:\nFiles to create/change (exact paths):\nTests first (list + key assertions):\nImplementation (each file as a path-labelled fenced code block):\nRun commands:\nNext step (1–3 items):\n\n## Approval/sign-off rules\n\n- Strategy parameter changes (DCA/Grid/TWAP/Trend/Mean reversion): requires Reviewer approval.\n- Risk-limit changes (max drawdown, daily loss, max notional, max positions): requires Reviewer approval + Auditor acknowledgement (Auditor role holder UNSPECIFIED).\n- UI-only cosmetic changes (labels/layout) that do not affect behaviour: Reviewer approval recommended; policy **UNSPECIFIED**.\n- Anything related to live trading: forbidden (must remain disabled; any attempt is rejected).\n\nIf a single person holds multiple roles, that must be explicitly declared as **UNSPECIFIED** and reviewed for conflict-of-interest risk.\n\n## On-call & incident response expectations\n\n- Operator is responsible for first response and safe shutdown.\n- Drills (tabletop) must be performed periodically (cadence UNSPECIFIED) covering:\n  - kill switch\n  - data stale\n  - repeated order rejections\n- Targets:\n  - RTO (restore time objective): UNSPECIFIED\n  - RPO (restore point objective): UNSPECIFIED\n\n## Discipline rules for researchers and operators\n\nResearchers\n- No “tuning in production”: parameters must be updated via draft→validate→apply and logged.\n- No acceptance without artefacts: every decision references run_id + metrics.json.\n- No overfitting shortcuts: walk-forward and sensitivity checks required (thresholds UNSPECIFIED).\n\nOperators\n- No manual DB edits for portfolio/config state (unless emergency procedure is explicitly documented; currently UNSPECIFIED).\n- If system behaviour is uncertain: do nothing, pause strategies, escalate.\n\n## UI controls for admin configuration\n\nUI must implement:\n- Draft vs Applied view\n  - Clear badges: “Draft”, “Pending approval”, “Applied (active)”.\n- Schema-driven forms\n  - Parameter types, bounds, units; invalid values cannot be saved.\n- Validation step\n  - Validate button shows errors/warnings before apply.\n- Diff and confirmation\n  - Show exact diff between active and draft.\n  - For risk-affecting changes: require typed confirmation (e.g., “APPLY RISK CHANGE”).\n- Approval workflow\n  - Reviewer approval required for behaviour/risk changes.\n- Rollback\n  - One-click rollback to previous applied config version.\n  - Rollback is audited and requires Reviewer approval (policy; may be UNSPECIFIED).\n- Audit trail\n  - Every action logged with user id, timestamp, config version, and diff.\n\n## Reviewer checks (exact, actionable)\n- [ ] Any config change uses draft→validate→apply; no direct writes to active config.\n- [ ] Active config is immutable; changes create a new version.\n- [ ] UI never displays or logs secrets; backend returns secrets only as masked placeholders.\n- [ ] Risk gates cannot be bypassed via UI (paper-only lock, drawdown/daily loss caps).\n- [ ] Rollback exists and is tested.\n- [ ] Every apply produces an audit record with diff.\n- [ ] Tests exist: validation rejects out-of-bounds; apply is atomic; audit event emitted.\n\n## Next steps (practical tasks for Cursor to implement)\n- Add persistent config versioning (DB table/model; storage choice UNSPECIFIED) and immutable audit log.\n- Add backend endpoints:\n  - create draft, update draft, validate draft, request approval, approve, apply, rollback, list audit events.\n- Add UI pages:\n  - Config list, Config editor (draft), Diff/Validate modal, Approvals queue, Audit log viewer.\n- Add tests:\n  - backend: schema validation, approval gating, atomic apply, rollback.\n  - frontend: form validation, diff rendering, approval flow, audit log display.\n- Define and document role mapping and approval rules (currently UNSPECIFIED for your organisation).\n"}