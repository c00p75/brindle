{"prompt":"# Brindle Production Reference Pack (Paper-Trading-First, Cursor-Enforceable)\n\nDate: 2026-04-05 (Africa/Lusaka)  \nStatus: **Paper trading only** (live trading work is forbidden until explicit safety gates are satisfied)  \nCI/CD provider: **UNSPECIFIED**  \nDeployment platform: **UNSPECIFIED**  \nAuth model: **UNSPECIFIED** (single-user vs multi-user)  \n\n## Executive summary\n\nThis document expands the project’s concise /docs into a single production-ready reference for engineering teams and Cursor-style LLM assistants. The enforcement model is “**contract-by-structure**”: hard safety rules (paper-only, no secrets, no network in tests) are implemented as **mechanically verifiable boundaries** (dependency injection, fakes/mocks, deterministic simulator) plus **review gates** (checklists + acceptance criteria).\n\nPrimary enforcement hooks are sourced from official tooling: FastAPI supports pytest-friendly in-process endpoint tests via `TestClient` and enables swapping dependencies in tests via `app.dependency_overrides` (a dict mapping original dependencies to override callables). citeturn1search2turn1search6turn1search3 Next.js defines environment-variable behaviour and the `NEXT_PUBLIC_` prefix convention for values exposed to the browser bundle, supporting a strict “no secrets in client” rule. citeturn0search2 CCXT’s async examples (`ccxt.async_support`, `fetch_ticker`, `exchange.close()`) support a runtime-only exchange adapter boundary. citeturn0search3 Pytest’s `monkeypatch.setenv`/`delenv` supports deterministic testing of env-driven safety flags without leaking state. citeturn1search0 Vitest’s mocking helpers (`vi.fn`, `vi.mock`, `vi.spyOn`, and `vi.stubGlobal`) support deterministic UI tests (e.g., stubbing `fetch`). citeturn1search1turn1search5\n\n---\n\n## Full product context\n\n### Goals\n\n- Build a **safe, testable, paper-trading-first** automated trading platform that supports gradual expansion from market data → paper execution → strategies → risk engine → persistence → observability.\n- Make it hard to “accidentally do the wrong thing” by enforcing safety invariants through architecture, tests, and reviewer gates.\n\n### Scope\n\nIn scope (production-ready, paper-only):\n- FastAPI backend + Next.js frontend.\n- Exchange market data reads through an adapter boundary.\n- Deterministic paper execution (ledger + simulator).\n- Baseline strategies (DCA, Grid, TWAP, trend-following, mean reversion) **as paper-only** runners.\n- Research artefacts (manifests, metrics.json, run logging).\n- Runbook + observability.\n\nOut of scope (forbidden or UNSPECIFIED):\n- Live trading order placement (forbidden).\n- Withdrawals/transfers (forbidden).\n- CI/CD vendor, deployment platform, secrets manager, monitoring backend (UNSPECIFIED).\n\n### Tech stack\n\n- Backend: **FastAPI** (Python 3.11+), Pydantic models, dependency injection. citeturn1search11\n- Frontend: **Next.js** App Router (TypeScript), environment variables via `.env` and `NEXT_PUBLIC_*`. citeturn0search2\n- Exchange adapter: **CCXT async_support** (runtime only). citeturn0search3\n- Storage: PostgreSQL (later), Redis (later) — both **UNSPECIFIED in early phases**.\n\n### Safety-first constraints\n\n- Paper trading only (all “place order” flows go to simulator).\n- TDD-first. No business logic without failing tests.\n- No secrets in code, tests, logs, or committed files.\n- No network in unit/integration tests (all external I/O mocked).\n\n---\n\n## Expanded algorithms reference\n\n### Shared conventions\n\nSymbol: `BASE/QUOTE` (e.g., BTC/USDT).  \n- Base asset: BTC; Quote asset: USDT.  \n- Portfolio base currency (for equity/PnL): typically quote currency (USDT), but can be configured (UNSPECIFIED default).\n\nAmounts:\n- Market **buy** uses `quote_amount` (spend quote currency).\n- Market **sell** uses `base_qty` (sell base currency quantity).\n\nPrecision/rounding policy: **UNSPECIFIED** (must be defined once and unit-tested).\n\n### Execution algorithms\n\n#### Market order (paper simulator)\n\nInputs:\n- `side`: buy|sell\n- `symbol`\n- `amount`: quote_amount for buy, base_qty for sell\n- `price`: reference price (from price feed)\n- `fee_bps`: basis points (e.g., 10 = 0.10%)\n- `slippage_bps`: basis points (may be 0; must be declared)\n\nDeterministic effective price:\n- Buy: `effective_price = price * (1 + slippage_bps/10_000)`\n- Sell: `effective_price = price * (1 - slippage_bps/10_000)`\n\nFee model (MVP):\n- Fee charged in quote currency on notional (policy; alternative fee currencies are **UNSPECIFIED**).\n- `fee_quote = notional_quote * fee_bps/10_000`\n\nFill quantities:\n- Buy: `base_qty = quote_amount / effective_price`\n- Sell: `quote_proceeds = base_qty * effective_price`\n\nLedger effects (fee-in-quote model):\n- Buy: `balances[QUOTE] -= quote_amount + fee_quote`; `positions[BASE].qty += base_qty`\n- Sell: `balances[QUOTE] += quote_proceeds - fee_quote`; `positions[BASE].qty -= base_qty`\n\nEdge cases:\n- Reject amount <= 0, price <= 0.\n- Reject insufficient funds (quote for buy, base for sell).\n- Reject if stale data flag set (see Data & Quality).\n\nWorked example (buy):\n- Starting: USDT=1000.00, BTC=0\n- Buy BTC/USDT with quote_amount=100.00, price=50,000, slippage=5 bps, fee=10 bps\n- effective_price = 50,000 * 1.0005 = 50,025\n- base_qty = 100 / 50,025 ≈ 0.0019990 BTC\n- fee_quote = 100 * 0.001 = 0.10 USDT\n- New: USDT = 1000 - 100 - 0.10 = 899.90; BTC ≈ 0.0019990\n\nWorked example (sell):\n- Starting: USDT=899.90, BTC=0.0019990\n- Sell base_qty=0.0010 BTC at price=50,000, slippage=5 bps, fee=10 bps\n- effective_price = 50,000 * 0.9995 = 49,975\n- proceeds = 0.0010 * 49,975 = 49.975 USDT\n- fee_quote = 49.975 * 0.001 ≈ 0.0500\n- Net credit ≈ 49.925\n- New: USDT ≈ 949.825; BTC ≈ 0.0009990\n\n#### Limit orders (later; simulator spec must exist)\n\nLimit order lifecycle (deterministic):\n- Create order (resting) at limit price.\n- Fill when market crosses or equals limit price.\n- Partial fills supported (see below).\nTime-in-force: **UNSPECIFIED** (GTC recommended for paper; test required).\n\n#### Partial fills (required for realism; can be phased)\n\nDeterministic model (example):\n- Each step/candle provides `available_liquidity_quote` or `max_fill_qty` (source UNSPECIFIED).\n- Fill `min(remaining_qty, max_fill_qty)` at deterministic price.\n- Emit multiple Fill records; update ledger per fill.\nIf partial fills are not implemented, document “market orders fill fully” as an explicit limitation.\n\n#### Retries and idempotency (runtime + simulator)\n\nPrinciple:\n- “If uncertain, do nothing.” Never duplicate orders.\n\n`client_order_id` policy:\n- Required for any order placement request.\n- Must be unique per (strategy_run_id, symbol, logical_order) — format **UNSPECIFIED**.\n- The execution boundary must be idempotent: same `client_order_id` returns the same result (fill or rejection) without double-applying ledger changes.\n\nSuggested idempotency store:\n- In-memory map (MVP), Redis later (UNSPECIFIED timeline).\n\nFailure semantics:\n- Validation failures: reject with error code + reason.\n- Idempotency hit: return previous result.\n- Missing price / stale data: reject (or NOOP at strategy layer) with explicit reason.\n\n### Position sizing algorithms\n\n#### Fixed fraction sizing (baseline)\n\nInputs:\n- `equity_base`\n- `risk_fraction` (e.g., 0.02)\n- `max_notional` (cap) — UNSPECIFIED default\n\nAlgorithm:\n- `notional = equity_base * risk_fraction`\n- clamp: `notional = min(notional, max_notional)`\n\nWorked example:\n- equity = 1000; risk_fraction = 0.02 → notional=20\n- if max_notional=50 → final notional=20\n\n#### Volatility-adjusted sizing (baseline)\n\nObjective: size inversely with volatility σ.\n\nEstimator choices (choose and document; initial default UNSPECIFIED):\n- Std dev of log returns over window N.\n- EWMA volatility.\n- ATR-like estimator (for candles).\n\nAlgorithm:\n- `raw_notional = risk_budget / max(sigma, epsilon)`\n- clamp to `max_notional`\n\nWorked example:\n- risk_budget=10; sigma=0.02 → raw=500\n- clamp max_notional=100 → final=100\n\nEdge cases:\n- sigma <= 0 → size=0 (NOOP), must be unit-tested.\n\n### Risk enforcement algorithms\n\nAll strategies must pass `risk_gate(...)` before every order.\n\nRequired gates (thresholds UNSPECIFIED; must be configured):\n- Kill switch: if enabled → reject all new orders.\n- Circuit breaker: if N consecutive execution/data failures → halt for cooldown.\n- Max drawdown: if drawdown_pct > MAX_DD_PCT → halt.\n- Daily loss limit: if today_pnl < -DAILY_LOSS_LIMIT → halt.\n- Max open positions: if count >= MAX_OPEN_POSITIONS → reject new positions.\n- Max notional per trade: cap sizing.\n- Max position per asset: cap inventory.\n\nDrawdown calculation:\n- Compute equity curve; drawdown at time t = (peak - equity[t]) / peak.\n\n### Canonical simple strategies (paper-only)\n\nFor each strategy: **goal**, **parameters**, **edge cases**, **expected ledger effects** (assuming market orders + fee-in-quote).\n\n#### DCA\n\nGoal: accumulate base asset by buying fixed quote amounts on a schedule.\n\nParameters:\n- symbol\n- interval_seconds\n- quote_amount_per_step\n- max_total_quote_spend\n- start/end time (optional; UNSPECIFIED default)\n- fee_bps, slippage_bps (policy)\n\nPseudocode:\n```text\nif now < next_due: NOOP\nif spent + quote_amount_per_step > max_total_quote_spend: STOP\nrisk_gate()\nprice = price_feed.last(symbol)\nif stale(price): NOOP/REJECT\nexecution.place_market_buy(symbol, quote_amount_per_step, price)\nspent += quote_amount_per_step\nnext_due = now + interval_seconds\n```\n\nExpected ledger effects per step:\n- QUOTE decreases by quote_amount + fee\n- BASE increases by quote_amount/effective_price\n\nEdge cases:\n- insufficient quote funds → reject; strategy must stop or backoff (policy UNSPECIFIED).\n\n#### Grid (simplified, market-only approximation)\n\nGoal: buy as price falls below grid levels and sell as price rises above levels.\n\nParameters:\n- ref_price (seed; else current price)\n- spacing_bps\n- levels\n- order_quote_amount (buy size)\n- take_profit policy (sell size) — UNSPECIFIED\n\nPseudocode:\n```text\np = price_last\nfor i in 1..levels:\n  buy_trigger  = ref_price * (1 - i*spacing_bps/10_000)\n  sell_trigger = ref_price * (1 + i*spacing_bps/10_000)\n\nif p <= next_buy_trigger and quote_balance >= order_quote_amount:\n  risk_gate(); buy(order_quote_amount)\nif p >= next_sell_trigger and base_position > min_base_to_sell:\n  risk_gate(); sell(base_qty)\n```\n\nExpected ledger effects:\n- buys increase base inventory; sells reduce it, capturing spread minus fees/slippage.\n\nEdge cases:\n- grid can accumulate inventory in downtrend; max position cap must block runaway.\n\n#### TWAP\n\nGoal: execute a target amount by slicing into equal parts over time.\n\nParameters:\n- side (buy|sell)\n- total_amount (quote for buy; base for sell)\n- slices\n- interval_seconds\n- max_price_deviation_bps (optional; UNSPECIFIED behaviour)\n\nPseudocode:\n```text\nif slice_index >= slices: STOP\nif now < next_due: NOOP\nslice_amount = total_amount/slices\nrisk_gate()\nprice = price_last\nexecution.place_market_order(side, symbol, slice_amount, price)\nslice_index += 1\nnext_due = now + interval_seconds\n```\n\nLedger effects:\n- repeated small fills; smoother inventory change.\n\n#### Trend-following (baseline)\n\nGoal: hold risk-on when trend is positive, exit when negative.\n\nParameters:\n- short_window, long_window (N) — UNSPECIFIED defaults\n- entry_size algorithm (fixed% or vol-adjusted)\n- exit behaviour (reduce to 0; partial) — UNSPECIFIED\n\nPseudocode:\n```text\nshort_ma = MA(prices, short_window)\nlong_ma  = MA(prices, long_window)\nif short_ma > long_ma and not in_position:\n  risk_gate(); buy(size)\nif short_ma < long_ma and in_position:\n  risk_gate(); sell(all or portion)\n```\n\nEdge cases:\n- whipsaw; use cooldown or hysteresis (UNSPECIFIED).\n\n#### Mean reversion (baseline)\n\nGoal: buy when price deviates below mean; sell when above.\n\nParameters:\n- lookback_window\n- entry_z, exit_z thresholds — UNSPECIFIED\n- sizing policy\n\nPseudocode:\n```text\nmu, sigma = mean_std(prices, window)\nz = (price - mu)/sigma\nif z <= -entry_z: buy(size)\nif z >= exit_z: sell(size or exit)\n```\n\nEdge cases:\n- sigma ~ 0; must NOOP.\n\n---\n\n## Research & evaluation framework\n\n### Research lifecycle (expanded)\n\n1) Strategy spec: hypothesis + failure modes + risk requirements.\n2) Define acceptance criteria (precommit thresholds; may be UNSPECIFIED but must exist).\n3) Parameter schema and bounds.\n4) Build unit tests (pure decision logic; boundary enforcement; risk blocks).\n5) Implement minimal strategy.\n6) Integration tests (strategy + simulator + ledger; offline).\n7) Run experiments via manifest.\n8) Produce results bundle (manifest + metrics.json + events summary).\n9) Review against acceptance criteria; decide Accept/Iterate/Reject; record in experiment log and/or graveyard.\n10) Add regression test for biggest observed failure.\n\n### Experiment manifest schema (JSON Schema, run_manifest.v1)\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"title\": \"RunManifest\",\n  \"type\": \"object\",\n  \"required\": [\"schema\",\"run_id\",\"strategy\",\"safety\",\"symbols\",\"params\",\"window\",\"data\",\"code\"],\n  \"properties\": {\n    \"schema\": { \"const\": \"run_manifest.v1\" },\n    \"run_id\": { \"type\": \"string\", \"minLength\": 1 },\n    \"strategy\": {\n      \"type\": \"object\",\n      \"required\": [\"strategy_id\",\"strategy_version\"],\n      \"properties\": {\n        \"strategy_id\": { \"type\": \"string\" },\n        \"strategy_version\": { \"type\": \"string\" }\n      }\n    },\n    \"safety\": {\n      \"type\": \"object\",\n      \"required\": [\"paper_trading_only\",\"live_trading_enabled\"],\n      \"properties\": {\n        \"paper_trading_only\": { \"const\": true },\n        \"live_trading_enabled\": { \"const\": false }\n      }\n    },\n    \"symbols\": { \"type\": \"array\", \"items\": { \"type\": \"string\" }, \"minItems\": 1 },\n    \"params\": { \"type\": \"object\" },\n    \"window\": {\n      \"type\": \"object\",\n      \"required\": [\"start_ms\",\"end_ms\"],\n      \"properties\": {\n        \"start_ms\": { \"type\": \"integer\" },\n        \"end_ms\": { \"type\": \"integer\" }\n      }\n    },\n    \"data\": {\n      \"type\": \"object\",\n      \"required\": [\"source\"],\n      \"properties\": {\n        \"source\": { \"type\": \"string\" },\n        \"version\": { \"type\": \"string\" }\n      }\n    },\n    \"code\": {\n      \"type\": \"object\",\n      \"required\": [\"version\"],\n      \"properties\": {\n        \"version\": { \"type\": \"string\" }\n      }\n    }\n  },\n  \"additionalProperties\": false\n}\n```\n\nUNSPECIFIED fields:\n- data.source/version values\n- code.version encoding (git hash recommended; UNSPECIFIED enforcement)\n\n### Run artefact layout (recommended)\n\n`experiments/<run_id>/`\n- `manifest.json` (run_manifest.v1)\n- `metrics.json` (metrics.v1)\n- `events.jsonl` (one JSON object per event) — format stable after observability choices\n- `notes.md` (optional)\n\n### metrics.json schema (metrics.v1, exact keys/types)\n\nExact keys (top-level; extras only under `extra`):\n- schema (const \"metrics.v1\")\n- run_id (string)\n- strategy_id (string)\n- strategy_version (string)\n- base_currency (string)\n- started_at_ms (int)\n- ended_at_ms (int)\n- starting_equity_base (number)\n- ending_equity_base (number)\n- gross_pnl_base (number)\n- total_fees_base (number)\n- net_pnl_base (number)\n- return_pct (number; percent)\n- max_drawdown_pct (number; percent)\n- trades_count (int)\n- equity_curve_points (int)\n- extra (object)\n\n(Full JSON Schema example is in Appendices.)\n\n### Walk-forward testing procedure (required)\n\nSplit the timeline into contiguous segments:\n- Train: derive parameters/heuristics (optimisation method UNSPECIFIED).\n- Validate: choose final parameter set; reject unstable strategies.\n- Forward: run “as-is” on unseen period (paper/replay).\n\nRules:\n- No tuning on forward segment.\n- Record manifest for each segment; compare metrics.\n\n### Sensitivity analysis (required)\n\nMinimal method:\n- One-at-a-time perturbation: vary each key parameter ±Δ (Δ UNSPECIFIED but stated), run same window.\n- Random scan: sample N parameter points (N UNSPECIFIED) within bounds.\n\nPass condition:\n- Performance should degrade smoothly; not collapse catastrophically on small changes.\n\n### Acceptance criteria templates\n\nPer strategy (fill thresholds; may be UNSPECIFIED until set):\n- Minimum run length: UNSPECIFIED\n- Minimum trades: UNSPECIFIED\n- Max drawdown (%): UNSPECIFIED\n- Return (%): UNSPECIFIED\n- Net expectancy after fees: > 0\n- Operational errors (rate): < UNSPECIFIED\n- Data staleness events: < UNSPECIFIED\n\n### Reproducibility checklist\n\n- [ ] Manifest saved and validated (schema).\n- [ ] Code version recorded.\n- [ ] Data source/version recorded.\n- [ ] Random seeds recorded (if used; UNSPECIFIED default).\n- [ ] metrics.json recorded and schema-valid.\n- [ ] events.jsonl recorded (no secrets).\n\n---\n\n## Execution & simulator specification\n\n### Execution boundary (contract)\n\nInterface (in-process; preferred):\n- `place_market_order(side, symbol, amount, price, client_order_id, ts_ms) -> OrderResult`\n\nOrderResult (deterministic):\n- order_id\n- client_order_id\n- status: filled|rejected\n- fill(s): list[Fill] (market may be length 1; partial fills later)\n- rejection_reason (if rejected)\n\nFailure semantics:\n- Invalid inputs → rejected with reason.\n- Unsafe flags → error (hard fail) or rejection (policy), but must be test-covered.\n- Idempotency: same client_order_id returns same OrderResult.\n\n### Fill model (deterministic rules)\n\n- Price: effective_price from slippage function (declared).\n- Fee: fee function (declared).\n- Timestamp: `ts_ms` provided by caller; if missing, server time (policy UNSPECIFIED).\n\n### Time handling\n\n- All timestamps: UTC epoch ms.\n- Time zone display in UI: UNSPECIFIED (recommended browser local).\n\n### Idempotency and client_order_id\n\n- Required input (`client_order_id` string).\n- Must be unique per logical order.\n- Execution boundary maintains mapping: client_order_id → OrderResult.\n\n### Minimal API contracts (OpenAPI-style)\n\nPOST `/paper/orders/market` (paper-only)\n```yaml\npost:\n  summary: Place a paper market order (paper only)\n  requestBody:\n    required: true\n    content:\n      application/json:\n        schema:\n          type: object\n          required: [client_order_id, side, symbol, amount, price, ts_ms]\n          properties:\n            client_order_id: { type: string }\n            side: { type: string, enum: [buy, sell] }\n            symbol: { type: string, example: BTC/USDT }\n            amount: { type: number, description: quote_amount for buy; base_qty for sell }\n            price: { type: number }\n            ts_ms: { type: integer }\n  responses:\n    '200':\n      description: Filled or idempotent replay\n    '400':\n      description: Validation error\n    '409':\n      description: Rejected (insufficient funds / risk gate)\n```\n\nGET `/paper/portfolio`\n```yaml\nget:\n  summary: Get paper portfolio snapshot\n  responses:\n    '200':\n      description: Current balances and positions\n```\n\n---\n\n## Data contracts & quality\n\n### Canonical schemas\n\nTicker (MVP):\n- symbol (string)\n- last (number > 0)\n- timestamp_ms (int > 0)\nOptional: bid/ask/volume (UNSPECIFIED)\n\nCandle (later):\n- symbol, timeframe\n- open/high/low/close (>0)\n- volume (>=0)\n- start_ts_ms, end_ts_ms (monotonic)\n\nTrade (later):\n- symbol, price (>0), qty (>0), timestamp_ms, side (optional)\n\n### Validation rules (must be enforced at boundaries)\n\n- Non-negative and non-zero prices/qty.\n- Staleness: (now_ms - timestamp_ms) <= MAX_DATA_AGE_MS (threshold UNSPECIFIED but configured).\n- Monotonic timestamps per symbol stream (no backwards).\n\n### Storage/replay recommendations\n\n- Store both raw and normalised forms (normalisation rules documented).\n- Provenance required:\n  - source (exchange/provider)\n  - collected_at_ms\n  - normaliser version (UNSPECIFIED)\n- Replay must ensure no lookahead (only past data available to strategy step).\n\n---\n\n## Observability & runbook\n\n### Structured logs (production requirement)\n\nAll logs must be structured (JSON objects), never free-text, and must **never** include secrets.\n\nRequired fields on every event:\n- `ts_ms` (UTC epoch ms), `level`, `event`, `component`, `request_id`, `strategy_id` (nullable), `run_id` (nullable)\n\nRequired event taxonomy (minimum):\n- Trading: `order_submitted`, `order_filled`, `order_rejected`, `ledger_updated`\n- Safety/risk: `safety_gate_failure`, `risk_limit_triggered`, `kill_switch_triggered` (future)\n- Data/exchange: `market_data_received`, `market_data_stale`, `exchange_error`\n\n(Example JSON events are provided in Appendices.)\n\n### Metrics (names + labels)\n\nMetrics backend/exporter: **UNSPECIFIED**. Names must be stable once introduced.\n\nCounters:\n- `paper_orders_submitted_total{strategy_id,symbol,side}`\n- `paper_orders_filled_total{strategy_id,symbol,side}`\n- `paper_orders_rejected_total{strategy_id,symbol,side,reason}`\n- `safety_gate_failures_total`\n- `exchange_errors_total{exchange_name,type}`\n- `risk_events_total{type}`\n\nGauges:\n- `market_data_age_ms{symbol}`\n- `portfolio_equity_base{run_id}`\n- `open_positions_count{run_id}`\n\nHistograms:\n- `api_request_latency_ms{route,method,status}`\n- `execution_latency_ms{strategy_id}`\n\n### Alerts (thresholds UNSPECIFIED)\n\nAlerting channel/provider: **UNSPECIFIED**.\n\nMinimum alert rules:\n- Any `safety_gate_failure` (page/urgent)\n- `market_data_age_ms > UNSPECIFIED` for `UNSPECIFIED` duration\n- `exchange_errors_total` spikes above `UNSPECIFIED` in `UNSPECIFIED` window\n- `paper_orders_rejected_total` spikes above `UNSPECIFIED` in `UNSPECIFIED` window\n- Risk halt triggered (`risk_limit_triggered`) (notify + require human review)\n\n### Runbook essentials (paper-only)\n\nPreflight (must pass before starting):\n- `PAPER_TRADING_ONLY=true`\n- `LIVE_TRADING_ENABLED=false`\n- `pytest` and `npm test` pass offline\n\nStart/stop:\n- Backend: start via `uvicorn` (or process manager **UNSPECIFIED**) and stop cleanly.\n- Frontend: `npm run dev` for local; prod hosting **UNSPECIFIED**.\n\nIncident response playbook:\n\nEmergency stop (any unsafe condition, unexpected behaviour, or runaway orders):\n1) Stop strategy runner (mechanism **UNSPECIFIED**).\n2) Enable kill switch (if implemented) and block new orders.\n3) Preserve artefacts: manifest + metrics.json + events.jsonl.\n4) Create post-incident review (template below).\n\nData failure (stale/missing prices):\n1) Pause strategies.\n2) Confirm staleness gate firing and `market_data_stale` events.\n3) Resume only after freshness is restored.\n\nRepeated execution failures (rejections/errors):\n1) Pause strategies.\n2) Inspect balances/positions and parameter units.\n3) Check risk gate reasons; fix and add tests.\n\nKey rotation (manual; secrets manager **UNSPECIFIED**):\n1) Create new exchange key (least privilege; no withdrawals).\n2) Update runtime env (never commit).\n3) Restart backend; verify `/health` and `/version`.\n4) Confirm logs contain no secrets.\n\nPost-incident review template:\n- Summary:\n- Impact:\n- Root cause:\n- Detection:\n- Resolution:\n- Preventative actions:\n- Tests added:\n- Docs/runbook updates:\n\n---\n\n## Testing & CI guidance\n\n### Test matrix\n\nUnit tests (required):\n- ledger invariants (balances never negative)\n- simulator fee/slippage calculations\n- risk gates (drawdown, daily loss, caps)\n- strategy decision logic (pure step; boundary call)\n- manifest/metrics schema validation\n\nIntegration tests (required):\n- API endpoints via FastAPI TestClient (offline).\n- Dependency overrides enforce fakes for exchange adapter. citeturn1search3\n\nE2E/UI tests (required minimal):\n- status/metrics UI renders with mocked fetch (Vitest stubGlobal). citeturn1search5\n\n### Mocking CCXT / ensuring no network\n\n- CCXT must be behind an `ExchangeClient` interface (adapter boundary).\n- Tests use `FakeExchangeClient`; API tests override the dependency via `app.dependency_overrides`. citeturn1search3\n- Optional hardening: add a “no network” pytest plugin (tool **UNSPECIFIED**) to fail tests on socket use.\n\n### Test data management (required)\n\nPrinciples:\n- Prefer small, deterministic fixtures over large datasets.\n- Any “golden” expected outputs must be versioned and reviewed.\n- Record random seeds (if used) in manifests and logs (seed policy **UNSPECIFIED**).\n\nRecommended artefacts:\n- `backend/tests/fixtures/` for small JSON fixtures (tickers, manifests).\n- `backend/tests/golden/` for known-good `metrics.json` outputs (only when stable).\n\n### Pytest snippets (dependency_overrides + monkeypatch)\n\n(See Appendices for runnable examples.)\n\n### CI checklist (provider UNSPECIFIED)\n\n- backend: typecheck (tool UNSPECIFIED), `pytest`\n- frontend: `npm test`, lint (tool UNSPECIFIED)\n- secret scan (tool UNSPECIFIED)\n- ensure paper-only defaults in `.env.example`\n\n---\n\n## Security & secrets handling\n\n### Secrets policy\n\n- Secrets never committed.\n- Secrets never logged.\n- Frontend secrets: forbidden; only `NEXT_PUBLIC_*` in browser. citeturn0search2\n\n### Env templates (minimum)\n\nBackend (`backend/.env.example`):\n```dotenv\nAPP_ENV=local\nLOG_LEVEL=INFO\nPAPER_TRADING_ONLY=true\nLIVE_TRADING_ENABLED=false\nDATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/tradingbot  # optional early\nREDIS_URL=redis://localhost:6379/0  # optional early\nEXCHANGE_NAME=UNSPECIFIED\nEXCHANGE_API_KEY=__REPLACE__\nEXCHANGE_API_SECRET=__REPLACE__\nEXCHANGE_API_PASSPHRASE=\n```\n\nFrontend (`frontend/.env.example`):\n```dotenv\nNEXT_PUBLIC_API_BASE_URL=http://localhost:8000\n```\n\nNotes:\n- Never commit real `.env` files.\n- Never put secrets in `NEXT_PUBLIC_*`. citeturn0search2\n\n### Least privilege exchange keys\n\n- Read/trade only; no withdrawals/transfers.\n- Permission verification procedure: UNSPECIFIED (manual required).\n\n### Logging redaction rules\n\n- Never include secret fields in structured logs.\n- If logging payloads, redact known fields: api_key, api_secret, passphrase, Authorization header.\n\nReviewer checks:\n- Diff contains no secrets.\n- No `NEXT_PUBLIC_` secrets.\n- Logs and tests do not print env vars.\n\n---\n\n## Reviewer checklists & gates\n\n### Per-phase checklist (high level)\n\nPhase 1 (skeleton):\n- endpoints /health, /version correct\n- default flags safe\n- UI status page tests\n\nPhase 2 (exchange mocks):\n- ExchangeClient boundary exists\n- API tests override dependency to fake\n- no CCXT in tests\n\nPhase 3 (paper ledger/sim):\n- deterministic simulator\n- insufficient funds + invalid inputs tested\n- portfolio endpoint returns snapshot\n\nPhase 4 (strategies paper + risk engine):\n- strategies call execution boundary only\n- risk gate enforced and test-covered\n- metrics.json produced per run\n\nPhase 5 (persistence/observability/realtime):\n- schema migrations (tool UNSPECIFIED)\n- event logging stable\n- alerts configured (thresholds UNSPECIFIED)\n\nPhase 6 (pre-live gates; live work still forbidden here):\n- all safety gates satisfied; separate design PR required\n\n### Mandatory safety gates before any live trading work (detailed)\n\nGate 0: separate “Live Trading Design” change (owner UNSPECIFIED)  \nGate 1: single execution boundary; strategies cannot bypass  \nGate 2: kill switch + hard limits implemented and tested  \nGate 3: observability & alerting operational (stack UNSPECIFIED)  \nGate 4: staging + dry-run validation (UNSPECIFIED environment)  \nGate 5: manual permission verification (no withdrawal) + security review\n\nIf any gate unmet: live trading remains forbidden.\n\n---\n\n## Deliverables & roadmap (phases 1–6)\n\n### Phase 1 — Project skeleton (paper-only foundation)\nDeliverables:\n- backend: /health, /version; settings; DI stub\n- frontend: /status\n- tests: pytest + vitest running offline\nAcceptance:\n- tests pass; defaults safe; no secrets\n\n### Phase 2 — Exchange service with mocks\nDeliverables:\n- ExchangeClient interface + FakeExchangeClient\n- market data endpoint (/market/ticker)\n- dependency_overrides tests\nAcceptance:\n- tests pass offline; no CCXT in tests\n\n### Phase 3 — Paper ledger + simulator\nDeliverables:\n- ledger, positions, fills\n- market order simulator (deterministic fee/slippage)\n- endpoints: /paper/orders/market, /paper/portfolio\nAcceptance:\n- deterministic tests; idempotency policy documented\n\n### Phase 4 — Strategy runner + research artefacts + risk engine (paper-only)\nDeliverables:\n- strategy interfaces; DCA/Grid/TWAP/trend/mean-reversion runners\n- run manifest + metrics.json generation\n- risk_gate (kill switch, drawdown, daily loss, caps)\nAcceptance:\n- strategies cannot bypass boundary; metrics schema validated; reproducible runs\n\n### Phase 5 — Persistence + realtime + observability hardening\nDeliverables:\n- persistent ledger storage (PostgreSQL) and/or event log store\n- realtime UI updates (WS/SSE UNSPECIFIED)\n- metrics export + alerting (stack UNSPECIFIED)\nAcceptance:\n- recovery works; runbook updated; alerts configured\n\n### Phase 6 — Pre-live readiness (live trading still forbidden)\nDeliverables:\n- complete gate evidence pack\n- dry-run environment proof\nAcceptance:\n- explicit sign-off; separate “live trading design” work item only after this\n\n---\n\n## Appendices\n\n### A. Master prompt output format (for any Cursor change)\n\n```text\nTask:\nAssumptions (mark UNSPECIFIED if needed):\nPlan:\nFiles to create/change (exact paths):\nTests first (list + key assertions):\nImplementation (each file as a path-labelled fenced code block):\nRun commands:\nNext step (1–3 items):\n```\n\n### B. JSON Schema — metrics.v1 (exact keys)\n\n```json\n{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"title\": \"Metrics\",\n  \"type\": \"object\",\n  \"required\": [\n    \"schema\",\"run_id\",\"strategy_id\",\"strategy_version\",\"base_currency\",\n    \"started_at_ms\",\"ended_at_ms\",\"starting_equity_base\",\"ending_equity_base\",\n    \"gross_pnl_base\",\"total_fees_base\",\"net_pnl_base\",\"return_pct\",\"max_drawdown_pct\",\n    \"trades_count\",\"equity_curve_points\",\"extra\"\n  ],\n  \"properties\": {\n    \"schema\": { \"const\": \"metrics.v1\" },\n    \"run_id\": { \"type\": \"string\" },\n    \"strategy_id\": { \"type\": \"string\" },\n    \"strategy_version\": { \"type\": \"string\" },\n    \"base_currency\": { \"type\": \"string\" },\n    \"started_at_ms\": { \"type\": \"integer\" },\n    \"ended_at_ms\": { \"type\": \"integer\" },\n    \"starting_equity_base\": { \"type\": \"number\" },\n    \"ending_equity_base\": { \"type\": \"number\" },\n    \"gross_pnl_base\": { \"type\": \"number\" },\n    \"total_fees_base\": { \"type\": \"number\" },\n    \"net_pnl_base\": { \"type\": \"number\" },\n    \"return_pct\": { \"type\": \"number\" },\n    \"max_drawdown_pct\": { \"type\": \"number\" },\n    \"trades_count\": { \"type\": \"integer\" },\n    \"equity_curve_points\": { \"type\": \"integer\" },\n    \"extra\": { \"type\": \"object\" }\n  },\n  \"additionalProperties\": false\n}\n```\n\n### C. Pytest examples (dependency_overrides + monkeypatch)\n\n```python\nfrom fastapi.testclient import TestClient\nfrom app.main import app, get_exchange_client\n\nclass FakeExchangeClient:\n    async def fetch_ticker(self, symbol: str) -> dict:\n        return {\"symbol\": symbol, \"last\": 1.0, \"timestamp\": 1}\n\ndef test_market_ticker_uses_fake_dependency():\n    app.dependency_overrides[get_exchange_client] = lambda: FakeExchangeClient()\n    client = TestClient(app)\n    r = client.get(\"/market/ticker\", params={\"symbol\": \"BTC/USDT\"})\n    assert r.status_code == 200\n    app.dependency_overrides.clear()\n```\n\n```python\nimport os\nimport pytest\n\ndef test_safe_flags(monkeypatch):\n    monkeypatch.setenv(\"PAPER_TRADING_ONLY\", \"true\")\n    monkeypatch.setenv(\"LIVE_TRADING_ENABLED\", \"false\")\n    assert os.getenv(\"PAPER_TRADING_ONLY\") == \"true\"\n    assert os.getenv(\"LIVE_TRADING_ENABLED\") == \"false\"\n```\n\n(Dependency overrides are documented as a simple dict on the FastAPI app. citeturn1search3)\n\n### D. Vitest example (stubGlobal fetch)\n\n```ts\nimport { vi, test, expect } from \"vitest\";\nvi.stubGlobal(\"fetch\", vi.fn(async () =>\n  new Response(JSON.stringify({ status: \"ok\" }), { status: 200 })\n));\ntest(\"fetch stub works\", async () => {\n  const r = await fetch(\"http://x/health\");\n  expect((await r.json()).status).toBe(\"ok\");\n});\n```\n\n(Vitest describes mocking utilities and `vi.stubGlobal`. citeturn1search5)\n\n### E. Observability examples\n\nEvent fields: `ts_ms, level, event, component, request_id, strategy_id, run_id, ...`\n\nOrder filled example:\n```json\n{\n  \"ts_ms\": 1710000000000,\n  \"level\": \"INFO\",\n  \"event\": \"order_filled\",\n  \"component\": \"paper_simulator\",\n  \"request_id\": \"req_123\",\n  \"strategy_id\": \"dca_btc\",\n  \"run_id\": \"run_456\",\n  \"symbol\": \"BTC/USDT\",\n  \"side\": \"buy\",\n  \"amount\": 100.0,\n  \"price\": 50000.0,\n  \"qty_filled\": 0.002,\n  \"fee\": 0.10,\n  \"fee_currency\": \"USDT\"\n}\n```\n\n### F. Mermaid diagrams\n\nArchitecture:\n```mermaid\nflowchart LR\n  UI[Next.js UI] -->|HTTP| API[FastAPI API]\n  API --> SVC[Services]\n  SVC --> EXS[ExchangeService]\n  EXS --> EXC[ExchangeClient Interface]\n  EXC -->|runtime only| CCXT[CCXT async_support]\n  SVC --> SIM[Paper Simulator]\n  SIM --> LED[Ledger]\n  LED --> REP[(Storage: UNSPECIFIED)]\n```\n\nPhase timeline:\n```mermaid\nflowchart TD\n  A[Phase 1: Skeleton] --> B[Phase 2: Exchange mocks]\n  B --> C[Phase 3: Paper simulator]\n  C --> D[Phase 4: Strategies + Risk]\n  D --> E[Phase 5: Persistence + Observability]\n  E --> F[Phase 6: Pre-live gates]\n  F --> G[Live trading design (separate; gated)]\n```\n\n### G. Short changelog\n\n- v0.1: initial prompt pack and safety principles  \n- v0.2: phased prompts + risk/runbook/strategy templates  \n- v0.3: this consolidated production reference pack\n"}