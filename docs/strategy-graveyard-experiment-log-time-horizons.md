{"prompt":"```docs/RESEARCH_SYSTEM.md\n# RESEARCH SYSTEM — Strategy Development Pipeline (Paper-First, Test-First)\n\nDate: 2026-04-04 (Africa/Lusaka)  \nStatus: Active  \nCI/CD provider: UNSPECIFIED  \nExperiment runner/scheduler: UNSPECIFIED  \nBacktest/replay engine: UNSPECIFIED\n\n## Executive summary (primary sources)\nThis workflow is enforceable because it maps rules to stack capabilities: FastAPI supports pytest-friendly, in-process testing (TestClient) and dependency swapping via `app.dependency_overrides`; Next.js environment-variable conventions (notably `NEXT_PUBLIC_`) prevent secret exposure in client bundles; CCXT async examples support a runtime-only exchange adapter; pytest `monkeypatch` safely tests env-driven gates; Vitest mocking enables deterministic UI tests. (FastAPI docs: Testing; FastAPI docs: Testing Dependencies / dependency_overrides; Next.js docs: Environment Variables; CCXT docs: Async Python examples; Pytest docs: monkeypatch; Vitest docs: Mocking)\n\n## Purpose\nDefine a repeatable, auditable strategy research process that is paper-only, deterministic, and test-first.\n\n## Scope\nIn:\n- Strategy spec → tests → implementation → experiments → evaluation.\n- Required artefacts per strategy and per run.\n\nOut:\n- Live trading (forbidden).\n- Private exchange endpoints (forbidden).\n- Tool choice for backtesting/scheduling (UNSPECIFIED).\n\n## Non-negotiable rules\n- Paper only: all orders go through the paper execution boundary.\n- Single boundary: strategy calls `ExecutionService.place_market_order(...)` (in-process) OR POST `/paper/orders/market` (HTTP). Never CCXT from strategies.\n- No network in tests: always use fakes/mocks.\n- TDD: failing tests first.\n- Deterministic: same inputs → same outputs.\n- Safe flags for runs: PAPER_TRADING_ONLY=true and LIVE_TRADING_ENABLED=false.\n\n## Required artefacts per strategy (Definition of Done)\n- Strategy spec (markdown): hypothesis, regime assumptions, failure modes, risk constraints.\n- Parameter schema: names, types, units, bounds, defaults (defaults may be UNSPECIFIED).\n- Tests: unit (pure step + boundary call), integration (strategy + simulator + ledger).\n- Run manifest (per run): run_id, strategy_version, params, window, data source (UNSPECIFIED), code version (UNSPECIFIED), safety flags.\n- Results bundle (per run): metrics.json (see METRICS.md) + event log summary (format UNSPECIFIED).\n\n## Research lifecycle (required)\n1) Write spec + failure modes.\n2) Pre-commit acceptance criteria (thresholds UNSPECIFIED but written).\n3) Define parameter schema.\n4) Write failing unit tests.\n5) Implement minimum logic.\n6) Add offline integration tests.\n7) Run experiment from manifest.\n8) Produce results bundle.\n9) Decide: ACCEPT / ITERATE / REJECT; add regression test.\n\n## Bias-control checklist (complete per run)\n- [ ] No lookahead (only data available at timestamp).\n- [ ] No leakage (no tuning on evaluation window).\n- [ ] Fees included.\n- [ ] Slippage model stated (may be UNSPECIFIED; label results fee-only).\n- [ ] Stale-data handling blocks orders.\n- [ ] Tested on ≥2 regimes (definition UNSPECIFIED but stated).\n\n## Acceptance criteria template (fill BEFORE runs)\n- Min run length / sample size: UNSPECIFIED\n- Max drawdown (%): UNSPECIFIED\n- Net expectancy after fees: > 0\n- Return (%): UNSPECIFIED\n- Operational error rate: < UNSPECIFIED\n- Early stop conditions: UNSPECIFIED\n\n## Required templates/snippets\n\n### Strategy research ticket (copy/paste)\n- Strategy id:\n- Hypothesis:\n- Symbols:\n- Entry/exit logic:\n- Failure modes:\n- Risk constraints:\n- Params (bounds + defaults):\n- Acceptance criteria:\n\n### Run manifest (YAML template)\n~~~yaml\nrun_id: run_YYYYMMDD_001\nstrategy_id: example\nstrategy_version: v0\npaper_trading_only: true\nlive_trading_enabled: false\nsymbols: [BTC/USDT]\nparams:\n  example_param: UNSPECIFIED\nwindow:\n  start_ms: UNSPECIFIED\n  end_ms: UNSPECIFIED\ncode_version: UNSPECIFIED\ndata_source: UNSPECIFIED\n~~~\n\n### Pytest: enforce execution boundary + env gates (runnable)\n~~~python\nimport os\nimport pytest\n\nclass FakeExecution:\n    def __init__(self):\n        self.calls = []\n    def place_market_order(self, side, symbol, amount, price):\n        self.calls.append((side, symbol, amount, price))\n        return {'status': 'filled'}\n\nclass ExampleStrategy:\n    def __init__(self, symbol):\n        self.symbol = symbol\n    def step(self, now_ms, price_last, execution):\n        execution.place_market_order('buy', self.symbol, 1.0, price_last)\n\ndef assert_safe_mode():\n    paper = os.getenv('PAPER_TRADING_ONLY', 'true').lower() == 'true'\n    live = os.getenv('LIVE_TRADING_ENABLED', 'false').lower() == 'true'\n    if (not paper) or live:\n        raise RuntimeError('Unsafe mode')\n\ndef test_strategy_calls_execution_boundary_only():\n    exe = FakeExecution()\n    ExampleStrategy('BTC/USDT').step(0, 50_000.0, exe)\n    assert exe.calls == [('buy', 'BTC/USDT', 1.0, 50_000.0)]\n\ndef test_env_flags_guarded_with_monkeypatch(monkeypatch):\n    monkeypatch.setenv('PAPER_TRADING_ONLY', 'true')\n    monkeypatch.setenv('LIVE_TRADING_ENABLED', 'false')\n    assert_safe_mode()\n    monkeypatch.setenv('PAPER_TRADING_ONLY', 'false')\n    with pytest.raises(RuntimeError):\n        assert_safe_mode()\n~~~\n\n## Example run commands\n- cd backend\n- pytest\n- python -m app.tools.run_strategy --manifest manifests/run_YYYYMMDD_001.yaml  # runner module UNSPECIFIED\n\n## Reviewer checks\n- [ ] Spec + failure modes exist.\n- [ ] Acceptance criteria written before run.\n- [ ] Parameter schema with bounds; defaults may be UNSPECIFIED but explicit.\n- [ ] Unit tests prove boundary call (no CCXT).\n- [ ] Integration tests offline.\n- [ ] Results bundle contains metrics.json.\n\n## Next steps\n- Choose parameter schema format (JSON Schema vs Pydantic): UNSPECIFIED.\n- Define numeric thresholds for acceptance criteria: UNSPECIFIED.\n- Implement the manifest-driven runner that always outputs metrics.json.\n```\n```docs/METRICS.md\n# METRICS — Standard Definitions and `metrics.json` Schema\n\nDate: 2026-04-04 (Africa/Lusaka)  \nSchema: metrics.v1  \nPnL policy (realised vs mark-to-market): UNSPECIFIED  \nFee currency conversion: UNSPECIFIED  \nCI/CD provider: UNSPECIFIED\n\n## Executive summary (primary sources)\nMetrics must be deterministic and testable offline. FastAPI testing patterns and `app.dependency_overrides` support isolating external dependencies; Next.js env rules prevent leaking secrets in UI; CCXT async examples enable runtime-only exchange adapters; pytest `monkeypatch` supports env-driven policy tests; Vitest mocking supports deterministic UI metric rendering. (FastAPI docs: Testing; FastAPI docs: Testing Dependencies / dependency_overrides; Next.js docs: Environment Variables; CCXT docs: Async Python examples; Pytest docs: monkeypatch; Vitest docs: Mocking)\n\n## Purpose\nDefine the minimum metric set and a strict `metrics.json` schema so strategy evaluation and dashboards are consistent.\n\n## Scope\nIn: paper/backtest run metrics, schema enforcement, unit tests for key calculations.  \nOut: advanced stats (Sharpe/Sortino/VaR) unless added later (UNSPECIFIED).\n\n## Non-negotiable rules\n- `metrics.json` top-level keys must be EXACT (no extras; add future fields under `extra`).\n- Fees included.\n- Timestamps are UTC epoch ms.\n- Metric computation is unit-tested and offline.\n\n## Required metric artefact: `metrics.json`\nTop-level object must contain EXACTLY these keys and types:\n- schema: string (must equal `metrics.v1`)\n- run_id: string\n- strategy_id: string\n- strategy_version: string\n- base_currency: string\n- started_at_ms: int\n- ended_at_ms: int\n- starting_equity_base: number\n- ending_equity_base: number\n- gross_pnl_base: number\n- total_fees_base: number\n- net_pnl_base: number\n- return_pct: number (percentage)\n- max_drawdown_pct: number (percentage)\n- trades_count: int\n- equity_curve_points: int\n- extra: object\n\n## Metric definitions\n- net_pnl_base = ending_equity_base − starting_equity_base\n- total_fees_base = sum(fees) converted to base currency (policy UNSPECIFIED)\n- gross_pnl_base = net_pnl_base + total_fees_base\n- return_pct = (net_pnl_base / starting_equity_base) * 100 (edge cases UNSPECIFIED)\n- max_drawdown_pct = max peak-to-trough drawdown on equity curve * 100\n\n## Data inputs required\nMinimum recorded inputs per run:\n- starting_equity_base, ending_equity_base\n- equity curve points (length = equity_curve_points)\n- fills: price, qty, fee, fee_currency, timestamp_ms\n- mark-to-market source policy (UNSPECIFIED but recorded)\n\n## Implementation guidance (paths + tests)\nRecommended backend paths:\n- backend/app/analytics/metrics.py\n- backend/tests/test_metrics_drawdown.py\n- backend/tests/test_metrics_schema.py\n\nRecommended frontend paths (optional):\n- frontend/components/MetricsCard.tsx\n- frontend/components/__tests__/MetricsCard.test.tsx\n\n## Required templates/snippets\n\n### Pytest: runnable max_drawdown_pct\n~~~python\ndef max_drawdown_pct(curve):\n    peak = float('-inf')\n    max_dd = 0.0\n    for x in curve:\n        peak = max(peak, x)\n        if peak > 0:\n            max_dd = max(max_dd, (peak - x) / peak)\n    return max_dd * 100.0\n\ndef test_max_drawdown_pct_basic():\n    assert round(max_drawdown_pct([100.0, 120.0, 90.0, 110.0]), 6) == 25.0\n~~~\n\n### Vitest: UI metrics rendering (minimal)\n~~~ts\nimport { render, screen } from '@testing-library/react';\nimport { test, expect } from 'vitest';\n\nfunction MetricsCard({ netPnl, maxDd }: { netPnl: number; maxDd: number }) {\n  return (<div><div>Net PnL: {netPnl.toFixed(2)}</div><div>Max DD: {maxDd.toFixed(2)}%</div></div>);\n}\n\ntest('renders key metrics', () => {\n  render(<MetricsCard netPnl={12.3} maxDd={2.1} />);\n  expect(screen.getByText(/Net PnL:/i)).toBeInTheDocument();\n  expect(screen.getByText(/Max DD:/i)).toBeInTheDocument();\n});\n~~~\n\n## Example run commands\n- cd backend && pytest\n- cd frontend && npm test\n\n## Reviewer checks\n- [ ] `metrics.json` has EXACT keys + types; extras only under `extra`.\n- [ ] Definitions match this doc.\n- [ ] Drawdown calculation unit-tested.\n- [ ] No network required to compute metrics.\n\n## Next steps\n- Decide mark-to-market and PnL policy: UNSPECIFIED.\n- Decide fee conversion policy: UNSPECIFIED.\n- Add advanced metrics only after schema stabilises.\n```\n```docs/RUNBOOK.md\n# RUNBOOK — Operating the Paper-Trading Platform\n\nDate: 2026-04-04 (Africa/Lusaka)  \nDeployment: UNSPECIFIED  \nAuth model: UNSPECIFIED  \nAlerting channel: UNSPECIFIED\n\n## Executive summary (primary sources)\nOperational safety hinges on enforceable defaults and test isolation. FastAPI testing and dependency overrides support safe, offline validation; Next.js env rules reduce client-side secret exposure; CCXT async examples enable a runtime-only adapter; pytest `monkeypatch` safely tests env flags; Vitest mocking makes UI checks deterministic. (FastAPI docs: Testing; FastAPI docs: Testing Dependencies / dependency_overrides; Next.js docs: Environment Variables; CCXT docs: Async Python examples; Pytest docs: monkeypatch; Vitest docs: Mocking)\n\n## Purpose\nProvide repeatable procedures to start/stop the system, verify paper-only safety, and respond to incidents.\n\n## Scope\nIn: local operations + generic production runbook steps (deployment UNSPECIFIED).  \nOut: live trading operations (forbidden).\n\n## Non-negotiable rules\n- PAPER_TRADING_ONLY=true and LIVE_TRADING_ENABLED=false for all runs.\n- Stop immediately if safety flags are wrong.\n- No secrets in logs or repo.\n\n## Preflight checklist (copy/paste)\n- [ ] backend env: PAPER_TRADING_ONLY=true\n- [ ] backend env: LIVE_TRADING_ENABLED=false\n- [ ] frontend env: NEXT_PUBLIC_API_BASE_URL set\n- [ ] backend: pytest passes offline\n- [ ] frontend: npm test passes\n- [ ] Exchange key permissions verified manually (read/trade only): process UNSPECIFIED\n\n## Start/stop commands\nBackend start:\n- cd backend\n- python -m venv .venv && source .venv/bin/activate\n- pip install -r requirements.txt\n- pytest\n- uvicorn app.main:app --reload --port 8000\nBackend stop:\n- Ctrl+C (or process manager: UNSPECIFIED)\n\nFrontend start:\n- cd frontend\n- npm install\n- npm test\n- npm run dev\nFrontend stop:\n- Ctrl+C\n\n## Incident response (paper-only)\nEmergency stop (any unsafe condition):\n1) Stop strategy runner (mechanism UNSPECIFIED).\n2) Stop backend if misconfigured.\n3) Preserve logs + run manifest + metrics.json.\n4) Start post-incident review.\n\nData feed failure (stale/missing prices):\n1) Pause strategies.\n2) Verify market data timestamps and staleness gate.\n3) Resume only when data is fresh.\n\nRepeated execution failures (order rejections/errors):\n1) Pause strategies.\n2) Inspect balances/positions and risk limits.\n3) Validate params (amount/price units).\n\n## Key rotation (manual)\n1) Create new exchange key with least privilege (read/trade only).\n2) Update runtime environment (never commit).\n3) Restart backend.\n4) Verify /health and /version.\n5) Confirm logs contain no secrets.\n\n## Post-incident review template\n- Summary:\n- Impact:\n- Root cause:\n- Detection:\n- Resolution:\n- Preventative actions:\n- Tests added:\n- Runbook updates:\n\n## Required snippet: pytest safety defaults\n~~~python\nfrom app.core.settings import Settings\n\ndef test_defaults_are_safe():\n    s = Settings()\n    assert s.paper_trading_only is True\n    assert s.live_trading_enabled is False\n~~~\n\n## Reviewer checks\n- [ ] Any new long-running component includes start/stop steps here.\n- [ ] Safety flags are enforced and tested.\n- [ ] Incident steps updated when new failure modes introduced.\n\n## Next steps\n- Define strategy runner process management (systemd/docker/etc): UNSPECIFIED.\n- Define alerting channel and on-call: UNSPECIFIED.\n- Implement and document a kill switch (required before any live work).\n```\n```docs/OBSERVABILITY.md\n# OBSERVABILITY — Logs, Metrics, Alerts (Paper-First)\n\nDate: 2026-04-04 (Africa/Lusaka)  \nLog destination: UNSPECIFIED  \nMetrics backend: UNSPECIFIED  \nTracing stack: UNSPECIFIED\n\n## Executive summary (primary sources)\nObservability is enforceable when events are structured and test-covered. FastAPI testing and dependency overrides keep pipelines deterministic; Next.js env rules reduce client-side secret risk; CCXT async examples support an adapter boundary; pytest `monkeypatch` supports env-gate tests; Vitest mocking supports deterministic UI behaviour without real APIs. (FastAPI docs: Testing; FastAPI docs: Testing Dependencies / dependency_overrides; Next.js docs: Environment Variables; CCXT docs: Async Python examples; Pytest docs: monkeypatch; Vitest docs: Mocking)\n\n## Purpose\nMake paper orders, safety failures, and data issues visible and diagnosable without leaking secrets.\n\n## Scope\nIn: structured logs, metric names, alert rules.  \nOut: specific tooling choice (UNSPECIFIED).\n\n## Non-negotiable rules\n- No secrets in logs (API keys, secrets, passphrases, auth headers).\n- All order attempts/outcomes emit events.\n- Safety gate failures emit ERROR events.\n- Include request_id for correlation.\n\n## Event taxonomy\nRequired fields on every event:\n- ts_ms (int), level, event, component, request_id, strategy_id (nullable), run_id (nullable)\n\n## Example JSON logs\nOrder filled:\n~~~json\n{\n  \"ts_ms\": 1710000000000,\n  \"level\": \"INFO\",\n  \"event\": \"order_filled\",\n  \"component\": \"paper_simulator\",\n  \"request_id\": \"req_123\",\n  \"strategy_id\": \"dca_btc\",\n  \"run_id\": \"run_456\",\n  \"order_id\": \"ord_1\",\n  \"symbol\": \"BTC/USDT\",\n  \"side\": \"buy\",\n  \"amount\": 100.0,\n  \"price\": 50000.0,\n  \"qty_filled\": 0.002,\n  \"fee\": 0.10,\n  \"fee_currency\": \"USDT\"\n}\n~~~\n\nSafety gate failure:\n~~~json\n{\n  \"ts_ms\": 1710000000000,\n  \"level\": \"ERROR\",\n  \"event\": \"safety_gate_failure\",\n  \"component\": \"config\",\n  \"request_id\": \"req_999\",\n  \"strategy_id\": null,\n  \"run_id\": null,\n  \"reason\": \"PAPER_TRADING_ONLY is false\"\n}\n~~~\n\n## Required metrics names\nCounters:\n- paper_orders_submitted_total{strategy_id,symbol,side}\n- paper_orders_filled_total{strategy_id,symbol,side}\n- paper_orders_rejected_total{strategy_id,symbol,side,reason}\n- safety_gate_failures_total\n- exchange_errors_total{exchange_name,type}\n\nGauges:\n- market_data_age_ms{symbol}\n- portfolio_equity_base{run_id}\n\nHistograms:\n- api_request_latency_ms{route,method,status}\n- execution_latency_ms{strategy_id}\n\n## Alert rules (thresholds UNSPECIFIED)\n- Any safety_gate_failure event (page)\n- market_data_age_ms > UNSPECIFIED for UNSPECIFIED duration\n- exchange_errors_total spike > UNSPECIFIED in UNSPECIFIED window\n- paper_orders_rejected_total spike > UNSPECIFIED\n\n## Required templates/snippets\n\n### Pytest: ensure secrets are not logged\n~~~python\nimport logging\n\ndef test_no_secrets_in_logs(caplog):\n    caplog.set_level(logging.INFO)\n    secret = 'SHOULD_NOT_APPEAR'\n    logging.getLogger('app').info('event', extra={'api_key': secret})\n    assert secret not in caplog.text\n~~~\n\n### Vitest: frontend must not log secrets\n~~~ts\nimport { test, expect, vi } from 'vitest';\n\ntest('frontend avoids logging secrets (pattern)', () => {\n  const spy = vi.spyOn(console, 'log').mockImplementation(() => {});\n  expect(spy).toBeDefined();\n  spy.mockRestore();\n});\n~~~\n\n## Example run commands\n- cd backend && pytest\n- cd frontend && npm test\n\n## Reviewer checks\n- [ ] Order lifecycle events exist (submitted/rejected/filled) with required fields.\n- [ ] Safety gate failures logged as ERROR with reason.\n- [ ] No secrets in logs; tests updated when logging changes.\n- [ ] Metric names are documented before implementation.\n\n## Next steps\n- Choose log/metrics backend: UNSPECIFIED.\n- Implement request_id correlation end-to-end.\n- Add dashboards and alert routing: UNSPECIFIED.\n```\n```docs/DATA.md\n# DATA — Contracts, Validation, and Interfaces\n\nDate: 2026-04-04 (Africa/Lusaka)  \nPrimary exchange(s): UNSPECIFIED  \nHistorical data source: UNSPECIFIED  \nMAX_DATA_AGE_MS default: UNSPECIFIED\n\n## Executive summary (primary sources)\nData rules are enforceable when all consumers use a single boundary and validation is test-covered. FastAPI testing and dependency overrides keep data pipelines offline in tests; Next.js env rules restrict client-exposed configs; CCXT async examples support runtime-only adapters; pytest `monkeypatch` supports env-driven staleness thresholds; Vitest mocking supports deterministic UI behaviour. (FastAPI docs: Testing; FastAPI docs: Testing Dependencies / dependency_overrides; Next.js docs: Environment Variables; CCXT docs: Async Python examples; Pytest docs: monkeypatch; Vitest docs: Mocking)\n\n## Purpose\nDefine market-data contracts and validation so strategies are deterministic and protected from stale/invalid data.\n\n## Scope\nIn: ticker contract now; candle/trade later; validation rules; PriceFeed interface.  \nOut: full storage/ETL design (UNSPECIFIED).\n\n## Non-negotiable rules\n- UTC epoch ms timestamps.\n- Canonical symbol format BASE/QUOTE (e.g., BTC/USDT).\n- Strategies consume data only via PriceFeed/ExchangeService, never CCXT.\n- Orders must be blocked when data is stale.\n\n## Data contracts\nTicker (MVP, required):\n- symbol: string\n- last: number (>0)\n- timestamp_ms: int (>0)\n\nCandle (later): UNSPECIFIED\nTrade (later): UNSPECIFIED\n\n## Validation rules\n- last > 0\n- timestamp_ms > 0 and not backwards per symbol stream\n- staleness: (now_ms - timestamp_ms) <= MAX_DATA_AGE_MS (threshold UNSPECIFIED but configured)\n\n## Storage/interface recommendation\nPriceFeed API (recommended boundary):\n- get_last(symbol) -> Ticker\n- get_last_many(symbols) -> map[symbol, Ticker]\n\n## Required templates/snippets\n\n### Pytest: ticker validation\n~~~python\ndef validate_ticker(t):\n    assert isinstance(t['symbol'], str) and '/' in t['symbol']\n    assert t['last'] > 0\n    assert t['timestamp_ms'] > 0\n\ndef test_validate_ticker_ok():\n    validate_ticker({'symbol': 'BTC/USDT', 'last': 1.0, 'timestamp_ms': 1})\n~~~\n\n### Pytest: staleness gate\n~~~python\ndef is_stale(now_ms, ts_ms, max_age_ms):\n    return (now_ms - ts_ms) > max_age_ms\n\ndef test_staleness_gate():\n    assert is_stale(2000, 0, 1000) is True\n    assert is_stale(2000, 1501, 1000) is False\n~~~\n\n## Example run commands\n- cd backend && pytest\n\n## Reviewer checks\n- [ ] Ticker contract validated at boundary.\n- [ ] Staleness gate exists and is test-covered.\n- [ ] No strategy calls CCXT directly.\n- [ ] Any new data type includes contract + validation + tests.\n\n## Next steps\n- Decide MAX_DATA_AGE_MS default: UNSPECIFIED.\n- Choose historical data source + replay method: UNSPECIFIED.\n- Implement persistent store when needed (PostgreSQL/Redis): UNSPECIFIED.\n```\n"}